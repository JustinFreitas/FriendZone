<?xml version="1.0" encoding="utf-8"?>

<!-- 
  Please see the license.txt file included with this distribution for 
  attribution and copyright information.
-->

<root>
	<windowclass name="npc" merge="join">
		<sheetdata>
			<tabs_recordsheet name="tabs">
				<tabverticaltextoffsetx>3</tabverticaltextoffsetx>
			</tabs_recordsheet>
		</sheetdata>
		<tab merge="add">
			<name>cohorts</name>
			<resource>tab_cohorts</resource>
			<class>charsheet_cohorts</class>
			<embed />
		</tab>
	</windowclass>

	<windowclass name="npc_combat_top_2014" merge="join">
		<sheetdata>
			<basicnumber name="hp" merge="join">
				<script>
					function onInit()
						if super and super.onInit then
							super.onInit();
						end
						registerMenuItem(Interface.getString("menu_reparse_hitpoints"), "spell_heal", 3);
					end
					function onMenuSelection(selection)
						if selection == 3 then
							HpManagerFZ.updateNpcHitPoints(window.getDatabaseNode());
						end
						if super and super.onMenuSelection then
							super.onMenuSelection(selection);
						end
					end
				</script>
			</basicnumber>
		</sheetdata>
	</windowclass>
</root>
